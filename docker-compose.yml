version: '3.8'

services:
  # MySQL Database
  db:
    image: mysql:8.0
    platform: linux/amd64
    container_name: poker-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${DB_NAME:-poker_platform}
      MYSQL_USER: ${DB_USER:-poker_user}
      MYSQL_PASSWORD: ${DB_PASSWORD:-poker_password}
    ports:
      - "3310:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD:-rootpassword}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - poker-network

  # Redis - Distributed locking and caching
  redis:
    image: redis:7-alpine
    container_name: poker-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redis_password}
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - poker-network

  # Go Backend API
  backend:
    build:
      context: .
      dockerfile: platform/backend/Dockerfile
    container_name: poker-backend
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_USER: ${DB_USER:-poker_user}
      DB_PASSWORD: ${DB_PASSWORD:-poker_password}
      DB_NAME: ${DB_NAME:-poker_platform}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis_password}
      SERVER_PORT: 8080
      JWT_SECRET: ${JWT_SECRET:-change-this-secret-key-in-production}
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/tables", "||", "exit", "1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - poker-network

  # React Frontend
  frontend:
    build:
      context: ./platform/frontend
      dockerfile: Dockerfile
    container_name: poker-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health", "||", "exit", "1"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - poker-network

  # Loki - Log aggregation system
  loki:
    image: grafana/loki:2.9.0
    container_name: poker-loki
    restart: unless-stopped
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./observability/loki-config.yaml:/etc/loki/local-config.yaml
      - loki_data:/loki
    networks:
      - poker-network

  # Promtail - Log collector
  promtail:
    image: grafana/promtail:2.9.0
    container_name: poker-promtail
    restart: unless-stopped
    volumes:
      - ./observability/promtail-config.yaml:/etc/promtail/config.yaml
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock
    command: -config.file=/etc/promtail/config.yaml
    depends_on:
      - loki
    networks:
      - poker-network

  # Grafana - Metrics and logs visualization
  grafana:
    image: grafana/grafana:10.1.0
    container_name: poker-grafana
    restart: unless-stopped
    ports:
      - "3005:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-admin}
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./observability/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - loki
    networks:
      - poker-network

  # DbGate - Database management tool
  dbgate:
    image: dbgate/dbgate
    container_name: poker-engine-dbgate
    restart: unless-stopped
    ports:
      - "10080:3000"
    environment:
      - CONNECTIONS=poker_db
      - LABEL_poker_db=Poker Platform Database
      - SERVER_poker_db=db
      - USER_poker_db=${DB_USER:-poker_user}
      - PASSWORD_poker_db=${DB_PASSWORD:-poker_password}
      - PORT_poker_db=3306
      - ENGINE_poker_db=mysql@dbgate-plugin-mysql
      - DATABASE_poker_db=${DB_NAME:-poker_platform}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - poker-network

volumes:
  db_data:
    driver: local
  redis_data:
    driver: local
  loki_data:
    driver: local
  grafana_data:
    driver: local

networks:
  poker-network:
    driver: bridge
