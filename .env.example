# Database Configuration
DB_ROOT_PASSWORD=your_strong_root_password_here
DB_NAME=poker_platform
DB_USER=poker_user
DB_PASSWORD=your_strong_db_password_here

# Redis Configuration (for distributed locking)
REDIS_PASSWORD=your_strong_redis_password_here

# Backend Configuration
JWT_SECRET=your_very_long_and_random_jwt_secret_key_here_change_in_production

# Grafana Configuration
GRAFANA_USER=admin
GRAFANA_PASSWORD=your_strong_grafana_password_here

# Frontend Configuration (used by Docker Compose)
# Note: For production builds, update platform/frontend/.env.production instead
REACT_APP_API_URL=http://localhost:8080/api
REACT_APP_WS_URL=ws://localhost:8080/ws

# DbGate Configuration
# DbGate is accessible at http://localhost:10080
# Database connection is automatically configured using the DB_USER, DB_PASSWORD, and DB_NAME variables above
